# lesvi — agent guide

## Agent skills

### Issue tracker

Issues live in GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: read `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
